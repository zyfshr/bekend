import express from "express"; import cors from "cors"; import fs from "fs"; import path from "path"; import { fileURLToPath } from "url"; import XLSX from "xlsx"; import { Connection, Keypair, PublicKey, Transaction } from "@solana/web3.js"; import { getOrCreateAssociatedTokenAccount, createTransferInstruction } from "@solana/spl-token"; import dotenv from "dotenv"; dotenv.config(); const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); const PORT = process.env.PORT || 3001; const RPC_URL = process.env.SOLANA_RPC || "https://api.mainnet-beta.solana.com"; const connection = new Connection(RPC_URL, "confirmed"); console.log("🔗 Using RPC:", RPC_URL); // ===== Wallet 路径 ===== const WALLET_A_PATH = process.env.WALLET_A_PATH || path.join(__dirname, "walletA.json"); const WALLET_B_PATH = process.env.WALLET_B_PATH || path.join(__dirname, "walletB.json"); const WALLET_C_PATH = process.env.WALLET_C_PATH || path.join(__dirname, "walletC.json"); // ===== 加载 Keypair ===== function loadKeypair(filePath) { if (!fs.existsSync(filePath)) { console.error("❌ Wallet file missing:", filePath); return null; } return Keypair.fromSecretKey(Uint8Array.from(JSON.parse(fs.readFileSync(filePath)))); } const walletA = loadKeypair(WALLET_A_PATH); const walletB = loadKeypair(WALLET_B_PATH); const walletC = loadKeypair(WALLET_C_PATH); const APP_UPDATE_INFO = { version: "5.4.1", // 版本号 isForceUpdate: false, // 是否强制更新 downloadUrl: "http://192.168.0.105:3000/download-apk", // 下载链接 storeUrl: "", // 可留空或填写商店链接 changeLog: - 修复了一些 bug - 优化性能 - 增加新功能：自动同步 NFT , }; // ===== NFT Catalog ===== const NFT_CATALOG = [ { id: "A", name: "UnionKey DEX", description: "1x Token Rights", image: "/images/A.jpg" }, { id: "B", name: "UnionKey DEX Plus", description: "10x Token Rights", image: "/images/B.jpg" }, { id: "C", name: "UnionKey DEX Pro", description: "100x Token Rights", image: "/images/C.jpg" } ]; // ===== 内存 mint 池 ===== let availableMintsA = []; let availableMintsB = []; let availableMintsC = []; // ===== 加载钱包 mints ===== async function loadWalletMints(wallet, list) { if (!wallet) return []; const accounts = await connection.getParsedTokenAccountsByOwner(wallet.publicKey, { programId: new PublicKey("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"), }); const mints = accounts.value .filter(acc => { const info = acc.account.data.parsed.info.tokenAmount; return info.amount === "1" && info.decimals === 0; }) .map(acc => acc.account.data.parsed.info.mint); if (list === "A") availableMintsA = mints; if (list === "B") availableMintsB = mints; if (list === "C") availableMintsC = mints; console.log(✅ Loaded ${mints.length} mints for ${list}); } async function initMints() { await loadWalletMints(walletA, "A"); await loadWalletMints(walletB, "B"); await loadWalletMints(walletC, "C"); } // ===== Excel SN管理 ===== const EXCEL_PATH = path.join(__dirname, "data", "serials.xlsx"); let workbook, SERIALS_A, SERIALS_B, SERIALS_C; function loadSerials() { workbook = XLSX.readFile(EXCEL_PATH); SERIALS_A = XLSX.utils.sheet_to_json(workbook.Sheets["A"] || []); SERIALS_B = XLSX.utils.sheet_to_json(workbook.Sheets["B"] || []); SERIALS_C = XLSX.utils.sheet_to_json(workbook.Sheets["C"] || []); console.log("✅ Excel serials loaded"); } loadSerials(); function saveExcel() { XLSX.writeFile(workbook, EXCEL_PATH); } function findSerialRow(serial) { let row = SERIALS_A.find(r => r["SN编号"] === serial); if (row) return { type: "A", row }; row = SERIALS_B.find(r => r["SN编号"] === serial); if (row) return { type: "B", row }; row = SERIALS_C.find(r => r["SN编号"] === serial); if (row) return { type: "C", row }; return null; } // ===== Express setup ===== const app = express(); app.use(cors()); app.use(express.json()); app.use(express.static(path.join(__dirname, "public"))); app.get("/my-app-config", (req, res) => { res.json({ code: 0, data: APP_UPDATE_INFO }); }); app.get('/download-apk', (req, res) => { const filePath = path.join(__dirname, 'apk', 'unionkey4.9.0.apk'); res.download(filePath, 'unionkey.apk'); // 第二个参数是下载时的文件名 }); app.get("/nfts", (req, res) => { const data = NFT_CATALOG.map(item => ({ id: item.id, name: item.name, description: item.description, image: item.image, available: [availableMintsA, availableMintsB, availableMintsC][['A','B','C'].indexOf(item.id)].length })); res.json(data); }); app.get("/check-serial", (req, res) => { const { serial } = req.query; if (!serial) return res.status(400).json({ error: "Missing serial" }); const found = findSerialRow(serial); if (!found) return res.json({ valid: false }); const { type, row } = found; const canClaim = row["状态"] !== "已领取"; res.json({ valid: true, type, status: row["状态"], remark: row["备注"], canClaim, }); }); async function transferNft(wallet, mintAddress, userAddress) { const mintPub = new PublicKey(mintAddress); const userPub = new PublicKey(userAddress); const fromAta = await getOrCreateAssociatedTokenAccount(connection, wallet, mintPub, wallet.publicKey); const toAta = await getOrCreateAssociatedTokenAccount(connection, wallet, mintPub, userPub); const tx = new Transaction().add(createTransferInstruction(fromAta.address, toAta.address, wallet.publicKey, 1)); const sig = await connection.sendTransaction(tx, [wallet], { preflightCommitment: "confirmed" }); await connection.confirmTransaction(sig, "confirmed"); return sig; } app.post("/claim", async (req, res) => { const { userAddress, type, serial } = req.body; if (!userAddress || !type || !serial) return res.status(400).json({ error: "Missing params" }); const found = findSerialRow(serial); if (!found || found.type !== type) return res.status(403).json({ error: "Serial not valid" }); const { row } = found; if (row["状态"] === "已领取") return res.status(403).json({ error: "Serial already used" }); let wallet, pool; if (type === "A") { wallet = walletA; pool = availableMintsA; } if (type === "B") { wallet = walletB; pool = availableMintsB; } if (type === "C") { wallet = walletC; pool = availableMintsC; } if (!wallet) return res.status(400).json({ error: "Invalid type" }); if (pool.length === 0) return res.status(410).json({ error: "Sold out" }); const mintAddress = pool.shift(); try { const sig = await transferNft(wallet, mintAddress, userAddress); row["状态"] = "已领取"; row["备注"] = 已领取: ${new Date().toISOString()}; const updatedData = XLSX.utils.json_to_sheet( type === "A" ? SERIALS_A : type === "B" ? SERIALS_B : SERIALS_C, { header: ["序号","生成时间","SN编号","状态","备注"] } ); workbook.Sheets[type] = updatedData; saveExcel(); res.json({ success: true, mint: mintAddress, signature: sig, type }); } catch (err) { console.error(err); res.status(500).json({ error: err.message }); } }); app.get("/", (req, res) => { res.sendFile(path.join(__dirname, "public/index.html")); }); app.listen(PORT, "0.0.0.0", async () => { console.log(🚀 Server running at http://0.0.0.0:${PORT}); await initMints(); });